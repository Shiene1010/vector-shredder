# Vector Shredder

Minimal vector graphics playground and demo.

## Contents

- `index.html` — demo page
- `script.js` — interaction and rendering logic
- `style.css` — basic styles

## How to run

Open `index.html` in your browser or serve with a static server:

```bash
# using Python 3
python -m http.server 8000
# then open http://localhost:8000
```

## License

MIT — see LICENSE (not included).

## Notes

- `references/` is ignored via `.gitignore`.
